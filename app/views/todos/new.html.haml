.container.white.shadow
  %h3.gray Create a new Noat
  = form_for(@todo) do |f|
    %p= f.text_field :item, :placeholder => "Add new item"
    %p= f.text_field :description, :placeholder => "description"
    %p= f.submit